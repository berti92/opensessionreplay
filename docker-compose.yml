services:
  opensessionreplay:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - BASIC_AUTH_USER=admin
      - BASIC_AUTH_PASS=admin
    volumes:
      - ./db_data:/app/data
    restart: unless-stopped